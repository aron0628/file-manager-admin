# PostgreSQL 접속 정보
DB_HOST=localhost
DB_PORT=5432
DB_NAME=app_db
DB_USER=app_user
DB_PASSWORD=password          # 특수문자 그대로 입력 가능 (URL 인코딩 불필요)

# 파일 업로드 경로
UPLOAD_DIR=uploads

# 문서 파서 서버
PARSER_SERVER_URL=http://localhost:9997

# 업로드 제한
MAX_UPLOAD_SIZE_MB=50
